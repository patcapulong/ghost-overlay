{
  "name": "ghost-overlay-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for Ghost Overlay",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
